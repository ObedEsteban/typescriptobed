import { Logger, LoggerFactory } from 'path/to/logger';
import { BST } from './BST';
import { Empleado } from './Empleado';

class Ejemplo {
    private static readonly LOGGER: Logger = LoggerFactory.getLogger(Ejemplo);

    static main(): void {

    }

    private static localizar(bst: BST<Empleado>, id: number): void {

    }
}

