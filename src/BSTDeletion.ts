import { BST } from './BST';
import { Empleado } from './Empleado';

class BSTDeletion {
    static eliminarNodo(bst: BST<Empleado> | null, id: number): void {

    }

    private static eliminarImpl(nodo: BST<Empleado>, id: number): void {

    }

    private static obtenerMinimo(nodo: BST<Empleado>): BST<Empleado> {

        return new BST<Empleado>();
    }

    private static reemplazarNodo(nodoEliminar: BST<Empleado>, nodoReemplazo: BST<Empleado> | null): void {

    }
}
